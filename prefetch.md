1. キーは algorithm + area + hour（例: a1/kanagawa/2026-04-13T10:00:00+09:00）
2. 実行タイミングは「毎正時 + 数分遅延」（生成完了遅れ対策）
3. 失敗時は短いリトライ（指数バックオフ）＋前回成功分を保持
4. TTL は 1〜数時間、更新中も stale を返せるようにする
5. レスポンスに cached_at / source_time を持たせると UI 側判断が楽
